<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Secondary Project Management System",
  "short_name": "SPMS",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "logotransparent.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#FFFFFF",
  "background_color": "#aa8a6e",
  "display": "standalone",
  "orientation": "portrait"
}
